import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import ListStack from './config/router';

export default class App extends React.Component {
  render() {
    return (
      <ListStack />
    );
  }
}
